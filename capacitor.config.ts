import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'demo-capacitor-ionic-svelte',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
